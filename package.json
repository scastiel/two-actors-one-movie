{
  "name": "taom",
  "version": "0.1.0",
  "description": "Two actors one movie",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir -p dist ; cp -r public/* dist ; ./node_modules/.bin/lessc styles/app.less dist/app.css ; ./node_modules/.bin/browserify -t babelify node_modules/material-design-lite/material.min.js src/app.jsx --outfile dist/app.js",
    "watch": "./node_modules/.bin/nodemon --watch ./public --watch ./src --watch ./styles --ext js,jsx,css,less,html --exec 'npm run build'",
    "start": "./node_modules/.bin/http-server dist"
  },
  "author": "Sébastien Castiel <sebastien.castiel@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "material-design-lite": "^1.0.0-3",
    "react": "^0.13.3"
  },
  "devDependencies": {
    "babelify": "^6.1.3",
    "browserify": "^11.0.0",
    "http-server": "^0.8.0",
    "less": "^2.5.1",
    "nodemon": "^1.3.7"
  }
}
